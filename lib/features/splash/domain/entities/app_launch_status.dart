class AppLaunchStatus {
  final bool hasSelectedLanguage;
  final bool hasSelectedExpertise;
  final bool isLoggedIn;

  AppLaunchStatus({
    required this.hasSelectedLanguage,
    required this.hasSelectedExpertise,
    required this.isLoggedIn,
  });
}
